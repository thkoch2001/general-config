if [ -r $HOME/.myenvironment ]; then
  . $HOME/.myenvironment
fi

#this is executed too early here. rather let this be done by awesome config
#$HOME/bin/run-autoruns.sh

# kdm has problems, if dbus does not come up fast enough...
#echo "start sleep"
#sleep 3
#echo "end sleep"
