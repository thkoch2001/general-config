if [ -r $HOME/.myenvironment ]; then
  . $HOME/.myenvironment
fi
# kdm has problems, if dbus does not come up fast enough...
# sleep 5
$HOME/bin/dex -v -a || true
# xautolock &
# autocutsel -fork &
# emacs --daemon &

